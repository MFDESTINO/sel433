	ORG	0
	MOV	R0, #50h
	MOV	R1, #00h
	MOV	DPTR, #8200h
LOOP:	MOVX	A, @DPTR
	MOV	@R0, A
	INC	R0
	INC	DPTR
	INC	R1
	CJNE	A, #00h, LOOP
	MOV	R0, #50h
	MOV	DPTR, #8300h
LOOP2:	MOV	A, @R0
	MOVX	@DPTR, A
	INC	R0
	INC	DPTR
	CJNE	A, #00h, LOOP2
	DEC	R1
	MOV	40h, R1
	END